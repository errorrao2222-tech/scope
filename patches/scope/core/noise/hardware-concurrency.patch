--- a/chrome/browser/scope_flag_choices.h
+++ b/chrome/browser/scope_flag_choices.h
@@ -35,6 +35,8 @@ constexpr const char kMiddleClickAutoscr
 constexpr const char kScopeNoiseCommandLine[] = "scope-noise";
 constexpr const char kScopeNoiseCanvasCommandLine[] = "scope-noise-canvas";
 constexpr const char kScopeNoiseAudioCommandLine[] = "scope-noise-audio";
+constexpr const char kScopeNoiseCpuCoresCommandLine[] =
+    "scope-noise-hw-concurrency";
 
 }  // namespace scope
 
--- a/chrome/browser/scope_flag_entries.h
+++ b/chrome/browser/scope_flag_entries.h
@@ -43,6 +43,11 @@
      "Adds insignificant jitter to audio data to deceive AudioContext-based "
      "fingerprinting. Scope flag.",
      kOsAll, FEATURE_VALUE_TYPE(blink::features::kScopeNoiseAudio)},
+    {scope::kScopeNoiseCpuCoresCommandLine,
+     "[Scope Noise] Randomize number of CPU cores",
+     "Randomizes the number of cores returned by "
+     "`navigator.hardwareConcurrency` in a reasonable range. Scope flag.",
+     kOsAll, FEATURE_VALUE_TYPE(blink::features::kScopeNoiseCpuCores)},
     // core/network
     // ui
     // platform
--- a/content/browser/scope_noise/noise_token_data.cc
+++ b/content/browser/scope_noise/noise_token_data.cc
@@ -59,6 +59,8 @@ ScopeNoiseTokenData::ScopeNoiseTokenDa
       blink::ScopeNoiseToken(base::RandUint64());
   session_tokens_[blink::ScopeNoiseFeature::kAudio] =
       blink::ScopeNoiseToken(base::RandUint64());
+  session_tokens_[blink::ScopeNoiseFeature::kHardware] =
+      blink::ScopeNoiseToken(base::RandUint64());
 }
 
 // static
--- a/third_party/blink/common/features.cc
+++ b/third_party/blink/common/features.cc
@@ -60,6 +60,9 @@ BASE_FEATURE(kScopeNoiseCanvas,
 BASE_FEATURE(kScopeNoiseAudio,
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+// navigator.hardwareConcurrency noising for fingerprint protection.
+BASE_FEATURE(kScopeNoiseCpuCores, base::FEATURE_ENABLED_BY_DEFAULT);
+
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
 BASE_FEATURE(kAIPageContentIncludePopupWindows,
--- a/third_party/blink/public/common/features.h
+++ b/third_party/blink/public/common/features.h
@@ -45,6 +45,7 @@ BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kScopeNoise);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kScopeNoiseCanvas);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kScopeNoiseAudio);
+BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kScopeNoiseCpuCores);
 
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
--- a/third_party/blink/public/common/scope_noise/noise_token.h
+++ b/third_party/blink/public/common/scope_noise/noise_token.h
@@ -23,6 +23,7 @@ namespace blink {
 enum class ScopeNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // A wrapper class for a uint64_t noise token.
--- a/third_party/blink/public/mojom/scope_noise/noise_token.mojom
+++ b/third_party/blink/public/mojom/scope_noise/noise_token.mojom
@@ -8,6 +8,7 @@ module blink.mojom;
 enum ScopeNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // Typemapped to blink::ScopeNoiseToken
--- a/third_party/blink/renderer/core/execution_context/navigator_base.cc
+++ b/third_party/blink/renderer/core/execution_context/navigator_base.cc
@@ -9,6 +9,7 @@
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/frame/navigator_concurrent_hardware.h"
+#include "third_party/blink/renderer/core/scope_noise/scope_noise_hash.h"
 #include "third_party/blink/renderer/core/probe/core_probes.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_builder.h"
@@ -41,6 +42,52 @@ String GetReducedNavigatorPlatform() {
 #endif
 }
 
+constexpr const unsigned kMinNoisedCores = 2;
+constexpr const unsigned kMaxNoisedCores = 16;
+
+std::vector<unsigned> PossibleValuesForCoreCount(unsigned n) {
+  // Clamp the number of cores between [2..=16], as other
+  // configurations are less common.
+  n = std::clamp(n, kMinNoisedCores, kMaxNoisedCores);
+
+  // A machine with a odd number of CPUs is very uncommon.
+  // Floor it to the lower even number.
+  if (n % 2) {
+    n -= 1;
+  }
+
+  // If we can subtract some amount and still end up with
+  // a common CPU count (which is the case for most configurations)
+  // let's add it to the list of possible values.
+  std::vector<unsigned> candidates = {n};
+  if (n > 4) {
+    candidates.push_back(n - 4);
+  }
+
+  if (n > 2) {
+    candidates.push_back(n - 2);
+  }
+
+  return candidates;
+}
+
+unsigned MaybeNoiseCoreCount(ExecutionContext* execution_context, unsigned n) {
+  if (!base::FeatureList::IsEnabled(blink::features::kScopeNoiseCpuCores) ||
+      !execution_context ||
+      !execution_context->GetScopeNoiseTokens().contains(
+          ScopeNoiseFeature::kHardware)) {
+    return n;
+  }
+
+  auto token_hash =
+      ScopeNoiseHash(execution_context->GetScopeNoiseTokens().at(
+          ScopeNoiseFeature::kHardware));
+  token_hash.Update(n);
+
+  auto values = PossibleValuesForCoreCount(n);
+  return values[token_hash.GetValueBelow(values.size())];
+}
+
 }  // namespace
 
 NavigatorBase::NavigatorBase(ExecutionContext* context)
@@ -77,7 +124,7 @@ unsigned int NavigatorBase::hardwareConc
 
   probe::ApplyHardwareConcurrencyOverride(
       probe::ToCoreProbeSink(GetExecutionContext()), hardware_concurrency);
-  return hardware_concurrency;
+  return MaybeNoiseCoreCount(GetExecutionContext(), hardware_concurrency);
 }
 
 ExecutionContext* NavigatorBase::GetUAExecutionContext() const {
--- a/third_party/blink/public/common/scope_noise/noise_token_mojom_traits.h
+++ b/third_party/blink/public/common/scope_noise/noise_token_mojom_traits.h
@@ -30,6 +30,8 @@ struct BLINK_COMMON_EXPORT
         return blink::mojom::ScopeNoiseFeature::kCanvas;
       case blink::ScopeNoiseFeature::kAudio:
         return blink::mojom::ScopeNoiseFeature::kAudio;
+      case blink::ScopeNoiseFeature::kHardware:
+        return blink::mojom::ScopeNoiseFeature::kHardware;
     }
     NOTREACHED();
   }
@@ -41,6 +43,8 @@ struct BLINK_COMMON_EXPORT
         return blink::ScopeNoiseFeature::kCanvas;
       case blink::mojom::ScopeNoiseFeature::kAudio:
         return blink::ScopeNoiseFeature::kAudio;
+      case blink::mojom::ScopeNoiseFeature::kHardware:
+        return blink::ScopeNoiseFeature::kHardware;
     }
 
     NOTREACHED();
