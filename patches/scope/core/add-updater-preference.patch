--- a/components/scope_services/pref_names.h
+++ b/components/scope_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kScopeExtProxyEna
 inline constexpr char kScopeBangsEnabled[] =
     "scope.services.bangs";
 
+inline constexpr char kScopeUpdateFetchingEnabled[] =
+    "scope.services.browser_updates";
+
 inline constexpr char kScopeSpellcheckEnabled[] =
     "scope.services.spellcheck_files";
 
--- a/components/scope_services/scope_services_helpers.h
+++ b/components/scope_services/scope_services_helpers.h
@@ -22,11 +22,13 @@ bool ShouldAccessServices(const PrefServ
 
 COMPONENT_EXPORT(SCOPE) bool ShouldFetchBangs(const PrefService& prefs);
 COMPONENT_EXPORT(SCOPE) bool ShouldAccessExtensionService(const PrefService& prefs);
+COMPONENT_EXPORT(SCOPE) bool ShouldAccessUpdateService(const PrefService& prefs);
 COMPONENT_EXPORT(SCOPE) GURL GetServicesBaseURL(const PrefService& prefs);
 COMPONENT_EXPORT(SCOPE) GURL GetDummyURL();
 COMPONENT_EXPORT(SCOPE) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(SCOPE) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
 COMPONENT_EXPORT(SCOPE) GURL GetSpellcheckURL(const PrefService& prefs);
+COMPONENT_EXPORT(SCOPE) GURL GetBrowserUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(SCOPE) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(SCOPE) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/scope_services/scope_services_helpers.cc
+++ b/components/scope_services/scope_services_helpers.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/command_line.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "components/scope_services/pref_names.h"
@@ -15,6 +16,28 @@
 #include "url/url_constants.h"
 
 namespace scope {
+#if defined(ARCH_CPU_ARM64)
+#define CPU_ARCH_STR "arm64"
+#elif defined(ARCH_CPU_X86_64)
+#define CPU_ARCH_STR "x86_64"
+#endif
+
+#if defined(CPU_ARCH_STR) && (BUILDFLAG(IS_MAC) || BUILDFLAG(IS_WIN))
+#define UPDATES_SUPPORTED
+#endif
+
+#if defined(UPDATES_SUPPORTED)
+constexpr std::string_view kUpdateBaseUrl = "https://updates.scope.invalid/";
+constexpr const char kUpdatePath[] =
+#if BUILDFLAG(IS_MAC)
+    "mac/appcast-" CPU_ARCH_STR ".xml";
+#elif BUILDFLAG(IS_WIN)
+    "win/appcast.xml";
+#endif
+
+// Duplicated from scope_flag_choices.h to avoid importing it.
+constexpr const char kUpdateServerUrlCommandLine[] = "custom-update-server-url";
+#endif
 
 std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_) {
     if (user_url_.empty()) {
@@ -73,6 +96,11 @@ bool ShouldFetchBangs(const PrefService&
             prefs.GetBoolean(prefs::kScopeBangsEnabled);
 }
 
+bool ShouldAccessUpdateService(const PrefService& prefs) {
+  return ShouldAccessServices(prefs) &&
+         prefs.GetBoolean(prefs::kScopeUpdateFetchingEnabled);
+}
+
 GURL GetExtensionUpdateURL(const PrefService& prefs) {
     if (!ShouldAccessExtensionService(prefs)) {
         return GetDummyURL();
@@ -98,6 +126,24 @@ GURL GetSpellcheckURL(const PrefService&
     return GetServicesBaseURL(prefs).Resolve("/dict/");
 }
 
+GURL GetBrowserUpdateURL(const PrefService& prefs) {
+  if (!ShouldAccessUpdateService(prefs)) {
+    return GetDummyURL();
+  }
+
+#if defined(UPDATES_SUPPORTED)
+  const auto* command_line = base::CommandLine::ForCurrentProcess();
+  std::string baseUrl(kUpdateBaseUrl);
+  if (command_line->HasSwitch(scope::kUpdateServerUrlCommandLine)) {
+    baseUrl =
+        command_line->GetSwitchValueASCII(scope::kUpdateServerUrlCommandLine);
+  }
+  return GURL(baseUrl).Resolve(kUpdatePath);
+#else
+  return GetDummyURL();
+#endif
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kScopeServicesEnabled, observer);
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -99,6 +99,11 @@
                     sub-label="$i18n{scopeSpellcheckToggleDescription}">
                 </settings-toggle-button>
             </if>
+            <settings-toggle-button id="updatesToggleButton"
+                pref="{{prefs.scope.services.browser_updates}}"
+                label="$i18n{scopeUpdatesToggle}"
+                sub-label="$i18n{scopeUpdatesToggleDescription}">
+            </settings-toggle-button>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -2395,6 +2395,9 @@ void AddPrivacyStrings(content::WebUIDat
       {"scopeSpellcheckToggle", IDS_SETTINGS_SCOPE_SERVICES_SPELLCHECK_TOGGLE},
       {"scopeSpellcheckToggleDescription",
         IDS_SETTINGS_SCOPE_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION},
+      {"scopeUpdatesToggle", IDS_SETTINGS_SCOPE_SERVICES_UPDATE},
+      {"scopeUpdatesToggleDescription",
+        IDS_SETTINGS_SCOPE_SERVICES_UPDATE_DESCRIPTION},
       {"scopeOriginOverride", IDS_SETTINGS_SCOPE_SERVICES_OVERRIDE},
       {"scopeOriginOverrideDescription",
         IDS_SETTINGS_SCOPE_SERVICES_OVERRIDE_DESCRIPTION},
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -430,6 +430,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kScopeSpellcheckEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kScopeUpdateFetchingEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kScopeServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kScopeServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -232,6 +232,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterStringPref(prefs::kScopeServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kScopeDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kScopeServicesConsented, false);
+    registry->RegisterBooleanPref(prefs::kScopeUpdateFetchingEnabled, true);
   }
 
   registry->RegisterBooleanPref(
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1760,6 +1760,22 @@
     <message name="IDS_SETTINGS_SCOPE_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION" desc="Description of the for enabling/disabling of downloading spellcheck files">
       Scope will fetch dictionary files used for spell checking when requested. When disabled, spell checking will not work.
     </message>
+    <if expr="not is_linux">
+      <message name="IDS_SETTINGS_SCOPE_SERVICES_UPDATE" desc="Toggle for automatic update downloads">
+        Allow automatic browser updates
+      </message>
+      <message name="IDS_SETTINGS_SCOPE_SERVICES_UPDATE_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+        Scope will automatically check for updates and install them. This includes browser and component updates. We recommend keeping this setting enabled to ensure you get the latest features and security updates.
+      </message>
+    </if>
+    <if expr="is_linux">
+      <message name="IDS_SETTINGS_SCOPE_SERVICES_UPDATE" desc="Toggle for automatic update downloads">
+        Allow automatic component updates
+      </message>
+      <message name="IDS_SETTINGS_SCOPE_SERVICES_UPDATE_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+        Scope will automatically check for component updates and install them. We recommend keeping this setting enabled. On Linux, browser updates are handled by your distro's package manager. Please check for Scope package updates at least every week.
+      </message>
+    </if>
     <message name="IDS_SETTINGS_SCOPE_SERVICES_OVERRIDE" desc="Text input for overriding the Scope services server">
       Use your own instance of Scope services
     </message>
--- a/chrome/browser/scope_flag_choices.h
+++ b/chrome/browser/scope_flag_choices.h
@@ -27,6 +27,8 @@ constexpr const FeatureEntry::Choice kCh
     {kChannelBeta, kChannelCommandLine, kChannelBeta},
 };
 
+constexpr const char kUpdateServerUrlCommandLine[] = "custom-update-server-url";
+
 }  // namespace scope
 
 #endif /* CHROME_BROWSER_SCOPE_FLAG_CHOICES_H_ */
--- a/chrome/browser/scope_flag_entries.h
+++ b/chrome/browser/scope_flag_entries.h
@@ -19,6 +19,10 @@
     {scope::kChannelCommandLine, "Update channel",
      "Selects which update channel to use for update checking. Scope flag.",
      kOsAll, MULTI_VALUE_TYPE(scope::kChannelChoices)},
+    {scope::kUpdateServerUrlCommandLine, "Update server URL",
+     "Overrides the default URL for fetching updates. Should be used only for "
+     "testing. Scope flag.",
+     kOsDesktop, STRING_VALUE_TYPE(scope::kUpdateServerUrlCommandLine, "")},
     // core/network
     // ui
     // platform
