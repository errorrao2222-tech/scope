--- a/chrome/browser/scope_flag_entries.h
+++ b/chrome/browser/scope_flag_entries.h
@@ -70,6 +70,10 @@
      "middleboxes and TLS endpoints mishandle this behavior and may become "
      "inaccessible. Scope flag.",
      kOsAll, FEATURE_VALUE_TYPE(net::features::kScopeSplitClientHello)},
+    {"no-proxy-server", "Ignore system proxy",
+     "Ignores system proxy settings and always connects directly. This may "
+     "prevent access to networks that require a proxy. Scope flag.",
+     kOsAll, SINGLE_VALUE_TYPE(switches::kNoProxyServer)},
     // ui
     // platform
 #endif  // CHROME_BROWSER_SCOPE_FLAG_ENTRIES_H_
