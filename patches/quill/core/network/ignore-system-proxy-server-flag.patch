--- a/chrome/browser/quill_flag_entries.h
+++ b/chrome/browser/quill_flag_entries.h
@@ -70,6 +70,10 @@
      "middleboxes and TLS endpoints mishandle this behavior and may become "
      "inaccessible. Quill flag.",
      kOsAll, FEATURE_VALUE_TYPE(net::features::kQuillSplitClientHello)},
+    {"no-proxy-server", "Ignore system proxy",
+     "Ignores system proxy settings and always connects directly. This may "
+     "prevent access to networks that require a proxy. Quill flag.",
+     kOsAll, SINGLE_VALUE_TYPE(switches::kNoProxyServer)},
     // ui
     // platform
 #endif  // CHROME_BROWSER_QUILL_FLAG_ENTRIES_H_
