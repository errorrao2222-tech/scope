--- a/chrome/browser/quill_flag_choices.h
+++ b/chrome/browser/quill_flag_choices.h
@@ -35,6 +35,8 @@ constexpr const char kMiddleClickAutoscr
 constexpr const char kQuillNoiseCommandLine[] = "quill-noise";
 constexpr const char kQuillNoiseCanvasCommandLine[] = "quill-noise-canvas";
 constexpr const char kQuillNoiseAudioCommandLine[] = "quill-noise-audio";
+constexpr const char kQuillNoiseCpuCoresCommandLine[] =
+    "quill-noise-hw-concurrency";
 
 }  // namespace quill
 
--- a/chrome/browser/quill_flag_entries.h
+++ b/chrome/browser/quill_flag_entries.h
@@ -43,6 +43,11 @@
      "Adds insignificant jitter to audio data to deceive AudioContext-based "
      "fingerprinting. Quill flag.",
      kOsAll, FEATURE_VALUE_TYPE(blink::features::kQuillNoiseAudio)},
+    {quill::kQuillNoiseCpuCoresCommandLine,
+     "[Quill Noise] Randomize number of CPU cores",
+     "Randomizes the number of cores returned by "
+     "`navigator.hardwareConcurrency` in a reasonable range. Quill flag.",
+     kOsAll, FEATURE_VALUE_TYPE(blink::features::kQuillNoiseCpuCores)},
     // core/network
     // ui
     // platform
--- a/content/browser/quill_noise/noise_token_data.cc
+++ b/content/browser/quill_noise/noise_token_data.cc
@@ -59,6 +59,8 @@ QuillNoiseTokenData::QuillNoiseTokenDa
       blink::QuillNoiseToken(base::RandUint64());
   session_tokens_[blink::QuillNoiseFeature::kAudio] =
       blink::QuillNoiseToken(base::RandUint64());
+  session_tokens_[blink::QuillNoiseFeature::kHardware] =
+      blink::QuillNoiseToken(base::RandUint64());
 }
 
 // static
--- a/third_party/blink/common/features.cc
+++ b/third_party/blink/common/features.cc
@@ -60,6 +60,9 @@ BASE_FEATURE(kQuillNoiseCanvas,
 BASE_FEATURE(kQuillNoiseAudio,
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+// navigator.hardwareConcurrency noising for fingerprint protection.
+BASE_FEATURE(kQuillNoiseCpuCores, base::FEATURE_ENABLED_BY_DEFAULT);
+
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
 BASE_FEATURE(kAIPageContentIncludePopupWindows,
--- a/third_party/blink/public/common/features.h
+++ b/third_party/blink/public/common/features.h
@@ -45,6 +45,7 @@ BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kQuillNoise);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kQuillNoiseCanvas);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kQuillNoiseAudio);
+BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kQuillNoiseCpuCores);
 
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
--- a/third_party/blink/public/common/quill_noise/noise_token.h
+++ b/third_party/blink/public/common/quill_noise/noise_token.h
@@ -23,6 +23,7 @@ namespace blink {
 enum class QuillNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // A wrapper class for a uint64_t noise token.
--- a/third_party/blink/public/mojom/quill_noise/noise_token.mojom
+++ b/third_party/blink/public/mojom/quill_noise/noise_token.mojom
@@ -8,6 +8,7 @@ module blink.mojom;
 enum QuillNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // Typemapped to blink::QuillNoiseToken
--- a/third_party/blink/renderer/core/execution_context/navigator_base.cc
+++ b/third_party/blink/renderer/core/execution_context/navigator_base.cc
@@ -9,6 +9,7 @@
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/frame/navigator_concurrent_hardware.h"
+#include "third_party/blink/renderer/core/quill_noise/quill_noise_hash.h"
 #include "third_party/blink/renderer/core/probe/core_probes.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_builder.h"
@@ -41,6 +42,52 @@ String GetReducedNavigatorPlatform() {
 #endif
 }
 
+constexpr const unsigned kMinNoisedCores = 2;
+constexpr const unsigned kMaxNoisedCores = 16;
+
+std::vector<unsigned> PossibleValuesForCoreCount(unsigned n) {
+  // Clamp the number of cores between [2..=16], as other
+  // configurations are less common.
+  n = std::clamp(n, kMinNoisedCores, kMaxNoisedCores);
+
+  // A machine with a odd number of CPUs is very uncommon.
+  // Floor it to the lower even number.
+  if (n % 2) {
+    n -= 1;
+  }
+
+  // If we can subtract some amount and still end up with
+  // a common CPU count (which is the case for most configurations)
+  // let's add it to the list of possible values.
+  std::vector<unsigned> candidates = {n};
+  if (n > 4) {
+    candidates.push_back(n - 4);
+  }
+
+  if (n > 2) {
+    candidates.push_back(n - 2);
+  }
+
+  return candidates;
+}
+
+unsigned MaybeNoiseCoreCount(ExecutionContext* execution_context, unsigned n) {
+  if (!base::FeatureList::IsEnabled(blink::features::kQuillNoiseCpuCores) ||
+      !execution_context ||
+      !execution_context->GetQuillNoiseTokens().contains(
+          QuillNoiseFeature::kHardware)) {
+    return n;
+  }
+
+  auto token_hash =
+      QuillNoiseHash(execution_context->GetQuillNoiseTokens().at(
+          QuillNoiseFeature::kHardware));
+  token_hash.Update(n);
+
+  auto values = PossibleValuesForCoreCount(n);
+  return values[token_hash.GetValueBelow(values.size())];
+}
+
 }  // namespace
 
 NavigatorBase::NavigatorBase(ExecutionContext* context)
@@ -77,7 +124,7 @@ unsigned int NavigatorBase::hardwareConc
 
   probe::ApplyHardwareConcurrencyOverride(
       probe::ToCoreProbeSink(GetExecutionContext()), hardware_concurrency);
-  return hardware_concurrency;
+  return MaybeNoiseCoreCount(GetExecutionContext(), hardware_concurrency);
 }
 
 ExecutionContext* NavigatorBase::GetUAExecutionContext() const {
--- a/third_party/blink/public/common/quill_noise/noise_token_mojom_traits.h
+++ b/third_party/blink/public/common/quill_noise/noise_token_mojom_traits.h
@@ -30,6 +30,8 @@ struct BLINK_COMMON_EXPORT
         return blink::mojom::QuillNoiseFeature::kCanvas;
       case blink::QuillNoiseFeature::kAudio:
         return blink::mojom::QuillNoiseFeature::kAudio;
+      case blink::QuillNoiseFeature::kHardware:
+        return blink::mojom::QuillNoiseFeature::kHardware;
     }
     NOTREACHED();
   }
@@ -41,6 +43,8 @@ struct BLINK_COMMON_EXPORT
         return blink::QuillNoiseFeature::kCanvas;
       case blink::mojom::QuillNoiseFeature::kAudio:
         return blink::QuillNoiseFeature::kAudio;
+      case blink::mojom::QuillNoiseFeature::kHardware:
+        return blink::QuillNoiseFeature::kHardware;
     }
 
     NOTREACHED();
