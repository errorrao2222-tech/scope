--- a/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
+++ b/chrome/browser/spellchecker/spellcheck_hunspell_dictionary.cc
@@ -24,8 +24,10 @@
 #include "base/task/thread_pool.h"
 #include "base/threading/scoped_blocking_call.h"
 #include "build/build_config.h"
+#include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/spellchecker/spellcheck_service.h"
 #include "chrome/common/chrome_paths.h"
+#include "components/quill_services/quill_services_helpers.h"
 #include "components/spellcheck/browser/spellcheck_platform.h"
 #include "components/spellcheck/common/spellcheck_common.h"
 #include "components/spellcheck/common/spellcheck_features.h"
@@ -277,15 +279,26 @@ GURL SpellcheckHunspellDictionary::GetDi
   std::string bdict_file = dictionary_file_.path.BaseName().MaybeAsASCII();
   DCHECK(!bdict_file.empty());
 
-  static const char kDownloadServerUrl[] =
-      "https://redirector.gvt1.com/edgedl/chrome/dict/";
+  if (browser_context_) {
+    const Profile* profile = Profile::FromBrowserContext(browser_context_);
+    if (!profile) {
+      goto fail;
+    }
+
+    const PrefService* prefs = profile->GetPrefs();
+    if (!prefs) {
+      goto fail;
+    }
+
+    return quill::GetSpellcheckURL(*prefs)
+            .Resolve(bdict_file);
+  }
 
-  return GURL(std::string(kDownloadServerUrl) +
-              base::ToLowerASCII(bdict_file));
+fail:
+  return GURL("https://failed-getting-spellcheck.qjz9zk/");
 }
 
 void SpellcheckHunspellDictionary::DownloadDictionary(GURL url) {
-  return;
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   DCHECK(browser_context_);
 
--- a/components/quill_services/quill_services_helpers.cc
+++ b/components/quill_services/quill_services_helpers.cc
@@ -63,6 +63,11 @@ bool ShouldAccessExtensionService(const
             prefs.GetBoolean(prefs::kQuillExtProxyEnabled);
 }
 
+bool ShouldAccessDictionaryService(const PrefService& prefs) {
+    return ShouldAccessServices(prefs) &&
+            prefs.GetBoolean(prefs::kQuillSpellcheckEnabled);
+}
+
 bool ShouldFetchBangs(const PrefService& prefs) {
     return ShouldAccessServices(prefs) &&
             prefs.GetBoolean(prefs::kQuillBangsEnabled);
@@ -85,6 +90,14 @@ GURL GetWebstoreSnippetURL(const PrefSer
         base::StringPrintf("/ext/cws_snippet?id=%s", id));
 }
 
+GURL GetSpellcheckURL(const PrefService& prefs) {
+    if (!ShouldAccessDictionaryService(prefs)) {
+        return GetDummyURL();
+    }
+
+    return GetServicesBaseURL(prefs).Resolve("/dict/");
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kQuillServicesEnabled, observer);
--- a/components/quill_services/quill_services_helpers.h
+++ b/components/quill_services/quill_services_helpers.h
@@ -26,6 +26,7 @@ COMPONENT_EXPORT(QUILL) GURL GetService
 COMPONENT_EXPORT(QUILL) GURL GetDummyURL();
 COMPONENT_EXPORT(QUILL) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(QUILL) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
+COMPONENT_EXPORT(QUILL) GURL GetSpellcheckURL(const PrefService& prefs);
 COMPONENT_EXPORT(QUILL) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(QUILL) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/quill_services/pref_names.h
+++ b/components/quill_services/pref_names.h
@@ -33,6 +33,9 @@ inline constexpr char kQuillExtProxyEna
 inline constexpr char kQuillBangsEnabled[] =
     "quill.services.bangs";
 
+inline constexpr char kQuillSpellcheckEnabled[] =
+    "quill.services.spellcheck_files";
+
 }  // namespace prefs
 
 #endif  // COMPONENTS_QUILL_SERVICES_PREF_NAMES_H_
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -5312,6 +5312,8 @@ static_library("test_support") {
     deps += [
       "//chrome/browser/spellchecker",
       "//components/spellcheck/common",
+      "//components/quill_services",
+      "//chrome/browser/profiles:profile",
     ]
   }
   if (enable_bound_session_credentials) {
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -428,6 +428,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kQuillBangsEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kQuillSpellcheckEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kQuillServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kQuillServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -228,6 +228,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterBooleanPref(prefs::kQuillServicesEnabled, true);
     registry->RegisterBooleanPref(prefs::kQuillBangsEnabled, true);
     registry->RegisterBooleanPref(prefs::kQuillExtProxyEnabled, true);
+    registry->RegisterBooleanPref(prefs::kQuillSpellcheckEnabled, true);
     registry->RegisterStringPref(prefs::kQuillServicesOrigin, "");
     registry->RegisterBooleanPref(prefs::kQuillDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kQuillServicesConsented, false);
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -92,6 +92,13 @@
                 label="$i18n{quillBangsToggle}"
                 sub-label="$i18n{quillBangsToggleDescription}">
             </settings-toggle-button>
+            <if expr="use_renderer_spellchecker">
+                <settings-toggle-button id="spellcheckToggleButton"
+                    pref="{{prefs.quill.services.spellcheck_files}}"
+                    label="$i18n{quillSpellcheckToggle}"
+                    sub-label="$i18n{quillSpellcheckToggleDescription}">
+                </settings-toggle-button>
+            </if>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1300,6 +1300,9 @@
         <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
           Please check with your network administrator to make sure that the firewall is not blocking downloads from Google servers.
         </message>
+        <message name="IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_QUILL" desc="Error message when spell dictionary download fails more than once possibly due to a network policy or configuration.">
+          Please check that Quill services are enabled (including spell check downloads) and make sure that the firewall is not blocking downloads from Quill servers.
+        </message>
       </if>
     </if>
     <if expr="is_chromeos">
@@ -1751,6 +1754,12 @@
     <message name="IDS_SETTINGS_QUILL_SERVICES_BANGS_TOGGLE_DESCRIPTION" desc="Description of the toggle for enabling/disabling of downloading bangs">
       Quill will fetch a list of bangs that help you browse the Internet faster, such as !w or !gh. When disabled, bangs will not work.
     </message>
+    <message name="IDS_SETTINGS_QUILL_SERVICES_SPELLCHECK_TOGGLE" desc="Toggle for enabling/disabling of downloading spellcheck files">
+      Allow downloading dictionary files for spell checking
+    </message>
+    <message name="IDS_SETTINGS_QUILL_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION" desc="Description of the for enabling/disabling of downloading spellcheck files">
+      Quill will fetch dictionary files used for spell checking when requested. When disabled, spell checking will not work.
+    </message>
     <message name="IDS_SETTINGS_QUILL_SERVICES_OVERRIDE" desc="Text input for overriding the Quill services server">
       Use your own instance of Quill services
     </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -1386,7 +1386,7 @@ void AddLanguagesStrings(content::WebUID
       {"languagesDictionaryDownloadError",
        IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED},
       {"languagesDictionaryDownloadErrorHelp",
-       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP},
+       IDS_SETTINGS_LANGUAGES_DICTIONARY_DOWNLOAD_FAILED_HELP_QUILL},
 #endif
 #endif  // !BUILDFLAG(IS_CHROMEOS)
 #if BUILDFLAG(IS_CHROMEOS)
@@ -2392,6 +2392,9 @@ void AddPrivacyStrings(content::WebUIDat
       {"quillBangsToggle", IDS_SETTINGS_QUILL_SERVICES_BANGS_TOGGLE},
       {"quillBangsToggleDescription",
         IDS_SETTINGS_QUILL_SERVICES_BANGS_TOGGLE_DESCRIPTION},
+      {"quillSpellcheckToggle", IDS_SETTINGS_QUILL_SERVICES_SPELLCHECK_TOGGLE},
+      {"quillSpellcheckToggleDescription",
+        IDS_SETTINGS_QUILL_SERVICES_SPELLCHECK_TOGGLE_DESCRIPTION},
       {"quillOriginOverride", IDS_SETTINGS_QUILL_SERVICES_OVERRIDE},
       {"quillOriginOverrideDescription",
         IDS_SETTINGS_QUILL_SERVICES_OVERRIDE_DESCRIPTION},
--- a/chrome/common/features.gni
+++ b/chrome/common/features.gni
@@ -9,6 +9,7 @@ import("//build/config/features.gni")
 import("//components/compose/features.gni")
 import("//components/safe_browsing/buildflags.gni")
 import("//components/signin/features.gni")
+import("//components/spellcheck/spellcheck_build_features.gni")
 import("//components/supervised_user/buildflags.gni")
 import("//content/public/common/features.gni")
 import("//crypto/features.gni")
@@ -111,4 +112,5 @@ chrome_grit_defines = [
   "chrome_root_store_cert_management_ui=$chrome_root_store_cert_management_ui",
   "use_nss_certs=$use_nss_certs",
   "webnn_enable_graph_dump=$webnn_enable_graph_dump",
+  "use_renderer_spellchecker=$use_renderer_spellchecker",
 ]
